APP_NAME="CRM"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
APP_TIMEZONE=UTC
APP_LOCALE=ru

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=8889
DB_DATABASE=crm
DB_USERNAME=root
DB_PASSWORD=root
# MAMP: если по порту не подключается, раскомментируйте следующую строку:
# DB_SOCKET=/Applications/MAMP/tmp/mysql/mysql.sock

SESSION_DRIVER=file
SESSION_LIFETIME=120

CACHE_STORE=file
QUEUE_CONNECTION=sync

# Видеоконференции (Jitsi Meet). По умолчанию — публичный сервер.
# Свой сервер: https://meet.ваш-домен.ru (без слэша в конце)
JITSI_SERVER_URL=https://meet.jit.si

# Push-уведомления PWA (сгенерировать: php artisan pwa:vapid)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
